## This repo contains small projects completed at Codeup that pertain to time series analysis.  A few items that were observed include sales data for a large grocery store and log requests for a local API.